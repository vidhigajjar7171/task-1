import React,{useState} from 'react'

const List = (props) => {
    const [list,setlist]=useState([]);
    const listitems=()=>
    {
        set
    }
  return (
    <div>List</div>
  )
}

export default List